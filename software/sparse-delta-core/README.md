# sparse-delta-core

Core package for the `sparse_delta` project: gate functions, complexity scores, the two-pass inference driver, and any sparse_delta-specific losses or model wrappers.

Editable workspace member of the outer `sparse_delta` uv workspace.

Currently empty — populated as the design described in [`../../notes/sparse_local_correction.md`](../../notes/sparse_local_correction.md) is implemented.
